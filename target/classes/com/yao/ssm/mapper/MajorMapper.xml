<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yao.ssm.mapper.MajorMapper">
    <select id="findByAca_id" parameterType="int" resultType="Major">
        select * from Major where aca_id = #{value}
    </select>
</mapper>